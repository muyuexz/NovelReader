name: Android CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      # 固定 debug 签名 key：ubuntu runner 每次都是全新 VM，AGP 会现场随机生成
      # ~/.android/debug.keystore，导致每次构建签名指纹不同 → 覆盖安装报
      # INSTALL_FAILED_UPDATE_INCOMPATIBLE。这里从仓库还原一份固定 keystore。
      - name: Restore fixed debug keystore
        run: |
          mkdir -p "$HOME/.android"
          cp ci/debug.keystore "$HOME/.android/debug.keystore"
          chmod 600 "$HOME/.android/debug.keystore"
          keytool -list -v -keystore "$HOME/.android/debug.keystore" -storepass android | grep -i 'SHA256:' || true

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: NovelReader-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      # 把 APK 塞回仓库（走 codeload 通道下载），并顺手上传一份到 0x0.st 备用
      - name: Publish APK back to repo
        if: always()
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/*.apk dist/NovelReader-debug.apk
          ls -lh dist/ || true
          URL=$(curl -sS -F "file=@app/build/outputs/apk/debug/app-debug.apk" https://0x0.st || true)
          echo "0x0.st -> $URL"
          echo "$URL" > dist/url.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f dist/
          git commit -m "build: NovelReader-debug.apk [skip ci]" || true
          git push || true
